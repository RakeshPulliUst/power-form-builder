import RemoveCircleIcon from "@mui/icons-material/RemoveCircle";
import React from "react";

type Props = {};

const RemoveCircleIcon1 = (props: Props) => {
  return <RemoveCircleIcon />;
};

export default RemoveCircleIcon1;
