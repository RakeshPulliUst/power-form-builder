import { RenderEngineOptions } from "../index.types";

const BootstrapEngine = ({ builderJSON, submission }: RenderEngineOptions) => {
  return <>Form is being rendered using Bootstrap Engine</>;
};

export default BootstrapEngine;
